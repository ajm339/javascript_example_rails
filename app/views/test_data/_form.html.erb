<%= form_for(@test_datum) do |f| %>
  <% if @test_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_datum.errors.count, "error") %> prohibited this test_datum from being saved:</h2>

      <ul>
      <% @test_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rank %><br>
    <%= f.number_field :rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
